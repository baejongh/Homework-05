package edu.grinnell.sortingvisualizer.sort;
/*package edu.grinnell.sortingvisualizer.sort;

import org.junit.Test;
import static org.junit.Assert.*;

public class SortTest {

	final Integer[] tester = new Integer[] {5, 34, 21, 12, 65, 1};
	Integer[] answer = new Integer[] {1, 5, 12, 21, 34, 65};
	
	final Integer[] tester2 = new Integer[] {42, 3, 23, 12, 53, 1};
	Integer[] answer2 = new Integer[] {1, 3, 12, 23, 42, 53};
	
	final Integer[] tester3 = new Integer[] {3, 24, 11, 32, 55, 56};
	Integer[] answer3 = new Integer[] {3, 11, 24, 32, 55, 56};
	
	@Test
	public void testQuickSort() {
		Sort.quickSort(tester, 0, 5);
		assertArrayEquals(answer, tester);
		Sort.quickSort(tester2, 0, 5);
		assertArrayEquals(answer2, tester2);
		Sort.quickSort(tester3, 0, 5);
		assertArrayEquals(answer3, tester3);
	}
	@Test
	public void testMergeSort() {
		Sort.mergeSort(tester);
		assertArrayEquals(answer, tester);
		Sort.mergeSort(tester2);
		assertArrayEquals(answer2, tester2);
		Sort.mergeSort(tester3);
		assertArrayEquals(answer3, tester3);
	}
	@Test
	public void testBubbleSort() {
		Sort.bubbleSort(tester);
		assertArrayEquals(answer, tester);
		Sort.bubbleSort(tester2);
		assertArrayEquals(answer2, tester2);
		Sort.bubbleSort(tester3);
		assertArrayEquals(answer3, tester3);
	}

	@Test
	public void testInsertionSort() {
		Sort.insertionSort(tester);
		assertArrayEquals(answer, tester);
		Sort.insertionSort(tester2);
		assertArrayEquals(answer2, tester2);
		Sort.insertionSort(tester3);
		assertArrayEquals(answer3, tester3);
	}

	@Test
	public void testSelectionSort() {
		Sort.selectionSort(tester);
		assertArrayEquals(answer, tester);
		Sort.selectionSort(tester2);
		assertArrayEquals(answer2, tester2);
		Sort.selectionSort(tester3);
		assertArrayEquals(answer3, tester3);
	}

	@Test
	public void testCocktailSort() {
		Sort.cocktailSort(tester);
		assertArrayEquals(answer, tester);
		Sort.cocktailSort(tester2);
		assertArrayEquals(answer2, tester2);
		Sort.cocktailSort(tester3);
		assertArrayEquals(answer3, tester3);
	}

}
*/